# use pd.to_datetime to convert a column of strings to datetime format
# we can also use pd.to_datetime to convert a column of strings to datetime format and then we can use the dt accessor to extract the year, month, day, etc from the datetime column
# shifting is shifting the values of a column up or down by a specified number of periods
# resampling is changing the frequency of the time series data to a specified frequency like daily,
# Rolling is calculating the rolling mean, rolling sum, etc for a specified window size